"use client";

import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { AlertTriangle, ArrowUpRight, Boxes, CheckCircle2, FileCheck2, ListChecks } from "lucide-react";
import { AppShell } from "@/components/app-shell";
import { MetricCard } from "@/components/metric-card";
import { StatusBadge } from "@/components/status-badge";

type System={id:string;name:string;owner:string|null;department:string|null;risk_level:"Low"|"Medium"|"High"|null;status:"Healthy"|"Review"|null;evidence_score:number};
type Summary={systems:System[];evidence:{id:string;status:"Verified"|"Pending"|"Missing"}[];actions:{id:string;status:"Open"|"In progress"|"Complete"}[]};

export default function DashboardPage(){
 const [summary,setSummary]=useState<Summary>({systems:[],evidence:[],actions:[]}); const [error,setError]=useState("");
 useEffect(()=>{fetch("/api/summary",{cache:"no-store"}).then(r=>r.json()).then(p=>{setSummary(p);setError(p.error??"")}).catch(()=>setError("Unable to load live governance data."))},[]);
 const metrics=useMemo(()=>{const s=summary.systems;const highRisk=s.filter(x=>x.risk_level==="High").length;const review=s.filter(x=>x.status==="Review").length;const verified=summary.evidence.filter(x=>x.status==="Verified").length;const evidenceCoverage=summary.evidence.length?Math.round(verified/summary.evidence.length*100):0;const openActions=summary.actions.filter(x=>x.status!=="Complete").length;const governance=Math.max(0,Math.min(100,Math.round((s.length?(s.reduce((a,x)=>a+(x.evidence_score??0),0)/s.length)*.45:0)+(s.length?(1-highRisk/s.length)*35:35)+evidenceCoverage*.2)));return{highRisk,review,verified,evidenceCoverage,openActions,governance}},[summary]);
 const attention=[...(metrics.highRisk?[`${metrics.highRisk} high-risk systems require active governance`]:[]),...(metrics.review?[`${metrics.review} systems are currently under review`]:[]),...(metrics.openActions?[`${metrics.openActions} remediation actions remain open`]:[]),...(metrics.evidenceCoverage<100?[`${100-metrics.evidenceCoverage}% of evidence is not yet verified`]:[])].slice(0,4);
 return <AppShell><div className="mx-auto max-w-[1500px] space-y-8 px-6 py-8 xl:px-8"><div><p className="text-[10px] uppercase tracking-[0.18em] text-[#dc6b27]">NEURONBRIGHT</p><h1 className="mt-2 text-2xl font-semibold tracking-tight">Control Centre</h1><p className="mt-2 text-sm text-white/35">Your organisation&apos;s current AI governance posture.</p></div>{error&&<div className="rounded-lg border border-[#dc6b27]/20 bg-[#dc6b27]/[0.05] p-3 text-xs text-[#dc6b27]">{error}</div>}<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4"><MetricCard label="AI systems" value={String(summary.systems.length)} detail={`${metrics.highRisk} high risk`} icon={<Boxes size={17}/>}/><MetricCard label="Governance posture" value={`${metrics.governance}%`} detail={`${metrics.review} systems under review`} icon={<CheckCircle2 size={17}/>}/><MetricCard label="Evidence coverage" value={`${metrics.evidenceCoverage}%`} detail={`${summary.evidence.length-metrics.verified} not verified`} icon={<FileCheck2 size={17}/>}/><MetricCard label="Open actions" value={String(metrics.openActions)} detail="remediation items" icon={<ListChecks size={17}/>}/></div><div className="grid gap-6 xl:grid-cols-[1fr_360px]"><section className="overflow-hidden rounded-xl border border-white/[0.08] bg-[#0b0b0b]"><div className="flex items-center justify-between border-b border-white/[0.08] px-5 py-4"><div><h2 className="text-sm font-medium">AI systems</h2><p className="mt-1 text-xs text-white/25">Live database inventory.</p></div><Link href="/systems" className="flex items-center gap-1 text-xs text-[#dc6b27]">View inventory<ArrowUpRight size={13}/></Link></div><div className="divide-y divide-white/[0.07]">{summary.systems.slice(0,6).map(system=><Link key={system.id} href={`/systems/${system.id}`} className="grid gap-4 px-5 py-5 hover:bg-white/[0.025] md:grid-cols-[1fr_130px_100px_90px]"><div><p className="text-sm font-medium">{system.name}</p><p className="mt-1 text-xs text-white/25">{system.department??"—"} · {system.owner??"Unassigned"}</p></div><div><p className="text-[9px] uppercase text-white/20">Risk</p><div className="mt-1"><StatusBadge status={system.risk_level??"Low"}/></div></div><div><p className="text-[9px] uppercase text-white/20">Evidence</p><p className="mt-2 text-xs text-white/60">{system.evidence_score}%</p></div><div><p className="text-[9px] uppercase text-white/20">Status</p><div className="mt-1"><StatusBadge status={system.status??"Review"}/></div></div></Link>)}{!summary.systems.length&&<div className="p-12 text-center text-sm text-white/30">No AI systems in the organisation yet.</div>}</div></section><section className="rounded-xl border border-white/[0.08] bg-[#0b0b0b]"><div className="border-b border-white/[0.08] px-5 py-4"><h2 className="text-sm font-medium">Attention required</h2><p className="mt-1 text-xs text-white/25">Live governance signals</p></div><div className="space-y-1 p-3">{attention.length?attention.map(item=><div key={item} className="flex items-center gap-3 rounded-lg px-3 py-3"><AlertTriangle size={15} className="shrink-0 text-[#dc6b27]"/><span className="text-xs text-white/55">{item}</span></div>):<div className="px-3 py-8 text-xs text-emerald-400">No immediate governance signals.</div>}</div><div className="border-t border-white/[0.08] p-4"><Link href="/actions" className="inline-flex w-full items-center justify-center gap-2 rounded-lg bg-[#dc6b27] px-4 py-2.5 text-xs font-semibold text-black">Open Action Centre<ArrowUpRight size={13}/></Link></div></section></div><section className="rounded-xl border border-white/[0.08] bg-[#0b0b0b] p-6"><div className="flex items-start justify-between gap-5"><div><p className="text-[10px] uppercase tracking-[0.18em] text-white/25">Governance posture</p><h2 className="mt-3 text-lg font-medium">{metrics.governance}% organisation readiness</h2><p className="mt-1 text-xs text-white/30">Composite signal from live inventory, evidence and remediation.</p></div><span className="rounded-full border border-[#dc6b27]/20 bg-[#dc6b27]/10 px-3 py-1 text-[10px] font-medium text-[#dc6b27]">Live</span></div><div className="mt-6 h-2 overflow-hidden rounded-full bg-white/[0.06]"><div className="h-full rounded-full bg-[#dc6b27]" style={{width:`${metrics.governance}%`}}/></div><div className="mt-4 flex flex-wrap gap-5 text-[10px] text-white/25"><span>Inventory: {summary.systems.length}</span><span>High risk: {metrics.highRisk}</span><span>Evidence: {metrics.evidenceCoverage}%</span><span>Open actions: {metrics.openActions}</span></div></section></div></AppShell>;
}
